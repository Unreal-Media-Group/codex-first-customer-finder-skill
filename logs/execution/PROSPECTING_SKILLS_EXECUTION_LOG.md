# Prospecting Skills Execution Log

Append-only. Never record credentials, private contact information, real prospect data, authenticated URLs, or sensitive values.

Tracked entries record repository tasks. `sync-upstream.sh` writes repetitive machine-local events to the ignored `PROSPECTING_SKILLS_SYNC_LOG.local.md` so synchronization does not dirty the worktree.

## Entry template

```text
### YYYY-MM-DDTHH:MM:SSZ — actor
- Task:
- Phase: Phase 1
- Risk / mode: Green / local Build
- Approval: not required for repository-only reversible work
- Files touched:
- Commands and checks actually run:
- Result:
- Blockers or deferred scope:
```

### 2026-07-16T00:00:00Z — Codex
- Task: Build the complete local, dry-run Phase 1 Unreal prospecting skills program.
- Phase: Phase 1
- Risk / mode: Green / local Build
- Approval: user requested the repository-only implementation; no external action authorized.
- Files touched: Phase 1 governance, skills, shared core, fixtures, tests, installer, reports, and documentation.
- Commands and checks actually run: bootstrap remote checks completed; final verification results are recorded in the completion report for this task.
- Result: implementation in progress; this entry is finalized by the task's verified diff.
- Blockers or deferred scope: Phase 2+, external systems, schedules, creative generation, likeness use, and outreach remain deferred.

### 2026-07-17T00:30:02Z — Codex final verification
- Task: Complete and review the local Phase 1 Unreal prospecting skills build.
- Phase: Phase 1
- Risk / mode: Green / local Build
- Approval: user requested repository-only Phase 1 implementation; no external action authorized.
- Files touched: governance, two canonical skills, shared validators/schemas/renderers, synthetic fixtures, installer, tests, reports documentation, and this log; original `first-customer-finder/**` and `scripts/install.js` remained unchanged.
- Commands and checks actually run: bootstrap and final upstream checks; shell/Node/Python syntax; JSON schema parsing/composition; 34 synthetic unit/integration tests; both official skill validators; temporary-repository sync safety tests; original installer/report regression; four report renders; status/diff/security/privacy reviews.
- Result: Phase 1 checks passed; sample reports were generated under `/private/tmp/unreal-prospecting-phase1-samples/`; no commit or push was created.
- Blockers or deferred scope: `npm pack --dry-run` could not run because npm is not installed; Phase 2+, live data, schedules, creative/likeness generation, and outreach remain deferred.

### 2026-07-17T04:00:00Z — GPT-5.6 review pass
- Task: Review and harden the uploaded Phase 1 implementation before commit.
- Phase: Phase 1.1 correction
- Risk / mode: Green / local Build on an extracted review copy
- Approval: requested repository review and necessary edits; no external action authorized.
- Files touched: duplicate/history validation, campaign and result validation, schemas, tests, both canonical skills, fork documentation, upstream-owned root-file conflict reduction, and this log.
- Commands and checks actually run: baseline shell/Node/Python checks; 34 original tests; adversarial reproduction of ambiguous identity, active-client subbrand, cooldown bypass, and ineligible handoff defects; corrected 39-test suite; report and installer regressions pending final packaging pass.
- Result: critical Phase 1 eligibility and deduplication gaps corrected in the review copy.
- Blockers or deferred scope: live web research quality, Unreal OS integration, scheduling, creative generation, likeness use, and outreach remain deferred.

### 2026-07-17T05:00:00Z — GPT-5.6 final review verification
- Task: Verify and package the Phase 1.1 review corrections.
- Phase: Phase 1.1 correction
- Risk / mode: Green / local Build on an extracted review copy
- Approval: requested repository review and necessary edits; no external action authorized.
- Files touched: no additional functional scope beyond the Phase 1.1 correction set; removed generated caches and macOS metadata.
- Commands and checks actually run: shell syntax checks; Node syntax checks; Python compile checks; Draft 2020-12 schema meta-validation; campaign and run-result validation; 39 unit/integration tests; four shared report renders; two installed-skill wrapper report renders; original-only, UMG-only, Talent-only, both-Unreal, and all-skills installer regressions; generated-artifact cleanup.
- Result: all available checks passed. The official Codex `quick_validate.py` script was not installed in this review environment, so both skills must be rerun through that validator in Noah's Codex checkout before commit.
- Blockers or deferred scope: the uploaded archive intentionally excluded `.git`, so real-remote session-start scripts were validated through temporary repositories rather than Noah's actual fork. Live research, Unreal OS integration, scheduling, creative generation, likeness use, and outreach remain deferred.

### 2026-07-17T02:30:11Z — Codex
- Task: Design Prospecting Phase 2 Unreal OS data contracts.
- Phase: Phase 2
- Risk / mode: Green / local design-only Build
- Approval: Noah explicitly authorized Phase 2 contract design; no external or implementation action was authorized.
- Files touched: `docs/specs/UNREAL_OS_PROSPECTING_DATA_CONTRACT.md`; `docs/specs/UNREAL_OS_PROSPECTING_RLS_AND_SERVICE_BOUNDARY.md`; `docs/specs/UNREAL_OS_PROSPECTING_MIGRATION_PLAN.md`; `docs/specs/UNREAL_PROSPECTING_SKILLS_PROGRAM_ROADMAP.md`; `AGENTS.md`; `UNREAL_README.md`; and this execution log.
- Commands and checks actually run: mandatory `scripts/check-upstream.sh` and `scripts/sync-upstream.sh`; branch/status/remote/checkpoint inspection; complete Phase 1 and read-only Unreal OS authority reads; three non-empty-document checks; targeted semantic/entity/field coverage checks; normal 39-test suite (tests passed, then official validators stopped only on missing PyYAML); `PYTHONPATH=/tmp/unreal-prospecting-pyyaml bash tests/prospecting/run_all.sh` (39 tests and both official skill validators passed); five frozen JSON schema parses; shell syntax and Python compile checks followed by generated-cache cleanup; status/name/stat/check inspection; exact upstream/Phase 1 no-diff check; complete changed-file review; and path-only security/privacy review.
- Result: the three design-only contracts were authored; every field in the four Phase 1 JSON contracts is represented; all 15 entities have keys, constraints, lifecycle, audit, prohibited-content, and future-Supabase notes; RLS covers every entity and operation; service boundaries and additive migration/round-trip/forward-fix gates are specified; Phase 1 regression checks pass. Nothing was staged, committed, pushed, deployed, connected, or written to an external system.
- Blockers or deferred scope: the separate Unreal OS repository was accessed read-only, but its status changed concurrently from the captured 33-entry baseline to clean, so the required byte-for-byte status comparison did not pass and was not “fixed.” Independent Phase 2 review/finalization remains required. SQL, migrations, Supabase/Unreal OS implementation, APIs, UI, services, agents, runners, loops, schedules, real-data import, creative/likeness generation, advertisements, and outreach remain deferred.

### 2026-07-17T02:31:19Z — Codex final verification addendum
- Task: Record the final Unreal OS baseline comparison without altering that separate repository.
- Phase: Phase 2
- Risk / mode: Green / read-only verification
- Approval: not required for read-only comparison.
- Files touched: this execution log only.
- Commands and checks actually run: recaptured `/Users/test/Developer/unreal-os` porcelain status and compared it with the task-start baseline.
- Result: the task-start baseline contained 33 status entries and the final comparison contained 6; the external worktree continued changing concurrently after an intermediate clean observation. This prospecting task made no Unreal OS edits.
- Blockers or deferred scope: byte-for-byte Unreal OS status equality remains unverified/failed; no attempt was made to restore, discard, stage, or otherwise alter those unrelated changes.

### 2026-07-17T03:02:16Z — Codex Phase 2 correction and synchronization prerequisite
- Task: Correct synchronization governance, then correct the existing Phase 2 BU-state, RLS/service-boundary, and migration-validation contracts.
- Phase: Phase 2 design remains open pending independent finalization; the synchronization change is a narrow repository-governance prerequisite.
- Risk / mode: Green / local repository-only Build under the user-authorized one-time bootstrap exception.
- Approval: explicit user authorization for the dirty-no-op prerequisite and the existing Phase 2 correction paths; no implementation or external action authorized.
- Synchronization prerequisite files changed in this pass: `scripts/sync-upstream.sh`; `tests/prospecting/test_upstream_scripts.py`; `docs/UPSTREAM_SYNC.md`; `AGENTS.md`; and this execution log.
- Phase 2 contract files changed in this pass: `docs/specs/UNREAL_OS_PROSPECTING_DATA_CONTRACT.md`; `docs/specs/UNREAL_OS_PROSPECTING_RLS_AND_SERVICE_BOUNDARY.md`; and `docs/specs/UNREAL_OS_PROSPECTING_MIGRATION_PLAN.md`.
- Existing dirty files preserved without edits in this pass: `UNREAL_README.md` and `docs/specs/UNREAL_PROSPECTING_SKILLS_PROGRAM_ROADMAP.md`.
- Commands and checks actually run: `bash scripts/check-upstream.sh`; `bash -n scripts/check-upstream.sh scripts/sync-upstream.sh`; `python3 -m unittest -v tests.prospecting.test_upstream_scripts`; protected Git-state snapshots before/after `bash scripts/sync-upstream.sh`; `PYTHONPATH=/tmp/unreal-prospecting-pyyaml bash tests/prospecting/run_all.sh`; five `python3 -m json.tool` schema parses; targeted `rg`, `awk`, Phase 1 field-count, protected-path diff, privacy/security, status, diff, cached, and conflict checks.
- Synchronization result: dirty synchronized worktrees now pass only through a validated true no-op; any required branch switch, fast-forward, or merge still blocks on dirtiness, and interrupted operations always block. Nine targeted regressions passed. On the real dirty worktree the patched script returned `already synchronized (dirty no-op)` and branch, HEAD, `main`, `unreal`, porcelain status, staged diff, remote configuration, and origin refs were byte-for-byte unchanged; no interrupted-operation marker existed. Only the ignored local synchronization log changed.
- Contract defects corrected: removed global-account BU status JSON maps; defined explicit BU and client/partner state kind, governed runless human corrections, non-colliding keys, append/supersede history, deterministic current selection, exact bounded Phase 1 outreach-status snapshots, BU-only projections, typed validation/errors, a 15-by-7 principal-explicit RLS matrix, and the requested future migration tests.
- Verification result: targeted synchronization tests passed; the full suite passed 42 tests, comprising all prior 39 tests plus three net new synchronization regressions; both official validators reported `Skill is valid!`; all five frozen schemas parsed; field coverage remained campaign 28, history 25, result 47, and report 23; 15 entities and all 105 principal cells were present; no stale Phase 1 stop rule or mutable BU status map remained.
- Safety result: no unrelated dirty file was modified. Unreal OS was not accessed. Nothing was staged, committed, pushed, deployed, connected, sent, or written to an external system. No SQL, migration, service, API, UI, runner, schedule, loop, creative, likeness, advertisement, outreach, secret, personal contact data, or real prospect record was added.
- Blockers or deferred scope: Phase 2 remains incomplete until another independent reviewer validates/finalizes the three contracts. SQL/type choices, contact protection/digest design, artifact storage, exact RLS implementation, service implementation, and migration execution remain future decisions.

### 2026-07-17T17:16:26Z — Codex Phase 2 semantic closure correction
- Task: Correct the Phase 2 decision-row, legacy-history, imported-status, current-state, and finalization semantics after the preceding verification-only response.
- Phase: Phase 2 repository-only contract design remains open pending independent review and explicit finalization.
- Risk / mode: Green / local documentation-only Build; no implementation or external action authorized.
- Files changed in this pass: `docs/specs/UNREAL_OS_PROSPECTING_DATA_CONTRACT.md`; `docs/specs/UNREAL_OS_PROSPECTING_RLS_AND_SERVICE_BOUNDARY.md`; `docs/specs/UNREAL_OS_PROSPECTING_MIGRATION_PLAN.md`; and this execution log.
- Commands and checks actually run: mandatory `bash scripts/check-upstream.sh` and `bash scripts/sync-upstream.sh` validated an already-synchronized dirty no-op before edits; targeted contract searches and constructibility audits; `python3 -m unittest -v tests.prospecting.test_upstream_scripts`; `PYTHONPATH=/tmp/unreal-prospecting-pyyaml bash tests/prospecting/run_all.sh`; both official `quick_validate.py` commands; all five frozen-schema parses; recursive Phase 1 field counts; 15-by-7 RLS cell audit; shell syntax, diff-check, protected-path hash, staged, conflict, status, and targeted path reviews.
- Semantic correction result: decision fields are conditional by controlled kind and the nine-row materialization matrix is explicit; unconstructible legacy discovery/campaign/score/decision/signal/rejection data stays in a verified artifact-backed baseline; normalized legacy state/protection rows name every required source; imported Phase 1 status values round-trip exactly without a per-value limit; current state uses deterministic effective/recorded/UUID ordering while explicit supersession is reserved for governed runless human corrections; and Section 8 records seven finalization rulings instead of unresolved mapping questions.
- Verification result: nine synchronization regressions and the full 42-test suite passed; both official skill validators reported `Skill is valid!`; all five schemas parsed; recursive field coverage remained campaign 28, history 25, result 47, and report 23; all 15 entities and 105 principal cells remained explicit; semantic closure searches found no stale imported-512-byte rejection, universal supersession rule, or unresolved Section 8.
- Safety result: the six non-authorized dirty paths retained their exact pre-edit SHA-256 values. The mandated upstream check performed its read-only Git fetch; no external write occurred, no other external system was accessed, and Unreal OS was not accessed. Nothing was staged, committed, pushed, deployed, connected, or sent. No SQL, migration, service, API, UI, worker, loop, schedule, creative, likeness, advertisement, outreach, secret, personal data, cache, or generated artifact was added.
- Blockers or deferred scope: a redundant final rerun of the upstream check/sync was rejected before execution by the environment's escalation-usage limit; the mandatory pre-edit check and dirty no-op had already passed, and all local final checks completed. Independent review/finalization is still required. Executable schema/RLS/service/migration work, physical artifact storage, encryption/key custody, real-contact retention, and any Unreal OS or external integration remain later explicitly authorized work.

### 2026-07-17 — Prospecting Phase 2 independent finalization metadata
- Task: Record the completed independent-review closure state for Prospecting Phase 2.
- Phase: Phase 2 finalized design; Phase 3 and executable implementation remain unauthorized.
- Risk / mode: Green / local metadata-only repository update.
- Review result: the prompt architect independently reviewed the three contracts and all substantive Phase 2 exit gates passed.
- Files changed: closure metadata only in the three Phase 2 contracts, program roadmap, repository guidance, README, and this execution log; no entity definition, mapping semantic, RLS decision, service boundary, migration sequence, test, synchronization behavior, or Phase 1 implementation changed.
- Commands and checks actually run: mandatory upstream validation and dirty no-op synchronization; targeted regression and full Phase 1 suites; official skill validators; schema parses; field/matrix counts; shell syntax; current-state search; and Git status/diff checks.
- Safety result: the mandated upstream validation was the only external read; no external system was written, no Unreal OS access occurred, and nothing was staged, committed, pushed, deployed, connected, or sent.
- Blockers or deferred scope: final staging and commit remain Noah's decision. Phase 3, SQL, migrations, Supabase/Unreal OS connections, services, APIs, UI, workers, runners, schedules, creative, advertisements, likeness use, and outreach remain unauthorized.

### 2026-07-17 — Prospecting Phase 3 local Manual Mission Control implementation
- Task: Implement the independent Prospecting Phase 3 Manual Mission Control feature from finalized Phase 2 contract commit `cb470c2`.
- Phase / status: Phase 3 repository-local Build; implemented locally and pending independent review/finalization. Phase 4 remains unauthorized.
- Risk / mode: Green / reversible local Build with a temporary loopback application used only for verification.
- Implementation files: `apps/prospecting-mission-control/mission_control/__init__.py`; `apps/prospecting-mission-control/mission_control/__main__.py`; `apps/prospecting-mission-control/mission_control/application.py`; `apps/prospecting-mission-control/mission_control/web.py`; `fixtures/prospecting/phase3/prospects.json`; and `tests/prospecting/test_phase3_mission_control.py`.
- Documentation and policy files: `docs/PHASE_3_MISSION_CONTROL.md`; `docs/ARCHITECTURE.md`; `docs/specs/UNREAL_PROSPECTING_SKILLS_PROGRAM_ROADMAP.md`; `AGENTS.md`; `UNREAL_README.md`; and this append-only log.
- Architecture result: the standard-library application binds only to `127.0.0.1`; constructs full Phase 1 campaign configurations through the frozen validator; uses a replaceable repository interface with a deterministic process-local fixture adapter; records immutable campaign versions, append-only manual run facts and governed review events; enforces fixture actor, business-unit, protection, idempotency, and separation-of-duty rules; renders escaped HTML; and exposes only an inert, unregistered future-worker placeholder. State resets when the process stops.
- Migration decision: no migration or SQL file was authored because the active Phase 3 storage adapter is process-local and a speculative persistence artifact was not required. No migration was applied, linked, dry-run, or executed.
- Verification performed: mandatory clean-start and final dirty-no-op upstream checks; 26 targeted Phase 3 domain/HTTP/security tests; Python compilation; fixture JSON parsing; the full 68-test repository suite, including all 42 prior regressions; both official skill validators (`Skill is valid!`); shell syntax; allowed-path and frozen-contract checks; interrupted-operation, staged, conflict, diff, cache, privacy, secret, outbound-client, dynamic-execution, and generated-junk checks; and complete final-diff review.
- Browser/accessibility result: the in-app browser exercised UMG campaign creation, manual dry run, all three queues, run metadata and zero cost, evidence, non-destructive identity resolution, suppression denial, cross-actor approval, rejection, assignment, notes, Talent filtered/archetype behavior, named-person and rights/brand-safety denials, unmatched-filter no-results, and malformed/cross-BU failures. Desktop and 375-pixel layouts had no horizontal overflow; controls were labeled; keyboard focus was visible; status and alert semantics were present; the browser console had no warnings/errors; all form targets were loopback; and no active external resource request existed. The local server was stopped after QA.
- Safety result: the starting worktree was clean, so there was no pre-existing dirty state to attribute or modify. No finalized Phase 2 contract, frozen Phase 1 schema/skill/fixture/core file, upstream skill, installer, hook, remote, other repository, real record, private contact value, credential, or secret was changed or accessed. The repository-mandated upstream Git fetch was the only external read. No external system was written.
- Exclusions: no database connection, migration application, registered or executable worker, schedule, watcher, retry daemon, autonomous loop, live research, scraping, external API, creative, advertisement, likeness use, outreach capability, outreach fact, deployment, or Phase 4 behavior was implemented or executed.
- Git result: nothing was staged, committed, pushed, deployed, connected, or sent. Final staging and commit remain Noah's decision.

### 2026-07-19 — Prospecting Phase 3 governed state-model correction
- Task: Correct the two independently reported Phase 3 state-model defects and return the phase for another independent review.
- Phase / status: Phase 3 repository-local Build correction from base commit `cb470c2`; still pending independent review/finalization. Phase 4 remains unauthorized and was not started.
- Risk / mode: Green / reversible local Build with a temporary loopback application used only for verification.
- Review findings addressed: independent review found (1) incorrect governed decision precedence, where any approval outranked every rejection and cross-kind supersession was refused, so a later rejection could never become current; and (2) an ineffective identity-resolution projection, where a valid resolution left `possible_duplicate` in place and approval still reported that identity review had never been resolved. Both were reproduced before any edit and re-verified after.
- Correction result: review actions are now grouped into governed state domains matching the contract decision kinds, so `approve_deeper_research` and `reject` are mutually exclusive values of one decision stream. The current leaf is selected deterministically by effective time, recorded time, and stable event ID; any later decision of either kind must explicitly supersede that leaf; and stale, wrong-prospect, wrong-business-unit, wrong-state-domain, and invented supersession targets fail closed with a safe `409`. Identity resolution now projects `effective_duplicate_state` and `effective_matched_identity_id`, is surfaced in the queue and detail views, and drives approval eligibility: a resolved record is no longer described as ambiguous, and a remaining denial names the resolved duplicate instead of claiming resolution never occurred. History stays append-only, the synthetic source classification, global identity, matched-identity list, and fixture record are preserved, and no eligibility, suppression, cooldown, relationship, rights, brand-safety, named-talent, separation-of-duty, or business-unit protection was weakened.
- Files changed in this pass: `apps/prospecting-mission-control/mission_control/application.py`; `apps/prospecting-mission-control/mission_control/web.py`; `tests/prospecting/test_phase3_mission_control.py`; and this append-only log. No fixture, schema, skill, installer, synchronization script, contract, roadmap, or unrelated documentation changed.
- Regression result: nine new focused regression tests cover approve-then-reject, reject-then-approve with separation of duty, stale/wrong-prospect/wrong-business-unit/other-state-domain/invented supersession, timestamp-tie ordering by stable event ID, effective and correctable identity resolution, resolved-identity approval reasons, decision-form supersession targets, safe `409` corrections without partial mutation, and effective identity display. All nine were confirmed to fail against the pre-correction implementation in an isolated copy and to pass after it; the original 26 Phase 3 tests were unaffected by that reversal.
- Verification performed: mandatory `bash scripts/check-upstream.sh` and a validated `bash scripts/sync-upstream.sh` dirty no-op before editing; 35 targeted Phase 3 tests; Python compilation of the application, shared core, and tests; the full 77-test repository suite including all 42 prior regressions and the original 26 Phase 3 tests; both official skill validators (`Skill is valid!`); fixture JSON parsing; shell syntax; `git diff --check`; and complete final-diff review including untracked files.
- Browser/HTTP replay result: the in-app browser loaded the application over loopback, rendered the campaign form with correct labels, ARIA status regions, and hidden-field structure, and completed a real form submission creating a campaign version (`POST /campaigns` → `201`). Its render surface then reported a zero-size, hidden viewport, so screenshots were blank and coordinate clicks could not land; visual focus-ring and narrow-layout observation was therefore not possible and is not claimed. The browser network log showed requests to `127.0.0.1:8765` only and the console reported no messages. The complete workflow was then replayed at HTTP level against the same live server with real HTML form round-trips, scraping every CSRF token and supersession target from the server's own markup: 33 checks covering campaign creation, manual dry run, zero cost, approval as Rob of a Noah-proposed prospect, decision-form supersession targets, superseding rejection, superseding approval, exact identity resolution, effective identity display and denial reason, stale/missing/invented supersession failing without partial mutation, non-loopback Host rejection, CSRF enforcement, and cross-business-unit denial. All 33 passed. The server bound only to `127.0.0.1`, logged status codes without paths, queries, or form bodies, and was stopped afterwards with the listener confirmed released.
- Safety result: the repository-mandated upstream Git fetch was the only external read. No external system was accessed or written, no Unreal OS or database connection occurred, and no migration, registered or executable worker, scheduler, watcher, loop, network client, credential, secret, personal record, creative, likeness, advertisement, or outreach capability was added or executed. The five pre-existing dirty tracked paths outside this correction were not modified.
- Git result: nothing was staged, committed, pushed, deployed, connected, or sent. Final staging and commit remain Noah's decision.

### 2026-07-20 — Prospecting Phase 4 registered agent implementation
- Task: Implement the authorized Prospecting Phase 4 registered agent from finalized Phase 3 commit `8ea7c16`.
- Phase / status: Phase 4 repository-local Build; implemented locally and pending independent review/finalization. Phase 3 is recorded as independently finalized at `8ea7c16`. Phase 5 remains unauthorized.
- Risk / mode: Green / reversible local Build with a temporary loopback application and a scratch-directory SQLite state file used only for verification.
- Implementation files created: `apps/prospecting-mission-control/mission_control/store.py`; `apps/prospecting-mission-control/mission_control/agent.py`; `fixtures/prospecting/phase4/worker-scenarios.json`; `tests/prospecting/test_phase4_registered_agent.py`; `docs/PHASE_4_REGISTERED_AGENT.md`.
- Files modified: `apps/prospecting-mission-control/mission_control/web.py` (Phase 4 routes, registered-agent delegation, mobile hash-wrap fix, `--state-path`); `apps/prospecting-mission-control/mission_control/application.py` (inert placeholder removed); `tests/prospecting/test_phase3_mission_control.py` (integration-compatible adjustments only); `.gitignore` (local runtime state); `docs/ARCHITECTURE.md`; `docs/PHASE_3_MISSION_CONTROL.md`; `docs/specs/UNREAL_PROSPECTING_SKILLS_PROGRAM_ROADMAP.md`; `AGENTS.md`; `UNREAL_README.md`; this append-only log.
- Architecture result: one locally registered `brand-prospecting-agent` (contract 1.0.0) with server-owned business-unit skill bindings (`unreal-media-brand-prospector@1.0.0-phase1-frozen`, `unreal-talent-campaign-prospector@1.0.0-phase1-frozen`) and per-skill SHA-256 integrity manifests executes the existing Phase 3 deterministic fixture adapter behind a durable local SQLite control plane (additive schema v1, `BEGIN IMMEDIATE` transactions, foreign keys, uniqueness for idempotency/attempts/outputs/review tasks, durable counter-based deterministic IDs, no destructive SQL). Bounded state machine (queued/running/succeeded/failed_retryable/failed_terminal/timed_out/cancelled) with conditional atomic transitions; synchronous cooperative one-shot execution in the request thread with injected clock, cooperative timeout, exact zero-dollar cost cap failing closed, three-attempt cap with manual append-only retries, queued and cooperative cancellation with a single-winner success/cancel race, idempotent replay and safe 409 idempotency conflicts, atomic output+attempt+run+review-task+audit success transaction with rollback on any audit/storage fault, exactly one pending human-review task per successful run, redacted safe failure classes, and audited non-automatic startup recovery of interrupted running work. No thread, listener, scheduler, loop, network client, credential, creative, likeness, or outreach capability survives or exists.
- Migration decision: no external migration or schema was authored or applied; SQLite initialization is repository-local only, in temporary test directories, a scratch QA path, or the gitignored `apps/prospecting-mission-control/local_state/` directory. No runtime database, journal, or WAL file exists in the repository.
- Verification performed: mandatory `bash scripts/check-upstream.sh` and safe `bash scripts/sync-upstream.sh` (already synchronized, no mutation) before editing; baseline 35 Phase 3 and 77 total tests confirmed on clean `8ea7c16`; after implementation 34 targeted Phase 4 tests, 35 Phase 3 tests, and the complete 111-test suite all passed; both official skill validators (`Skill is valid!`); Python compilation; all fixture JSON parsing; shell syntax; `git diff --check`; SQLite `integrity_check`/`foreign_key_check` on the live QA state file (ok, no violations, schema version 1); scoped searches over the changed files for credentials, personal contact data, network clients, shell/dynamic execution, schedulers/loops, creative/outreach, destructive SQL, sleeps, and absolute paths (only test negative-assertion strings matched); and complete final-diff review including untracked files.
- Browser/HTTP QA result: the in-app browser exercised UMG campaign creation, registered-agent record and capability pages, a manual registered-agent run to `succeeded · review pending` with attempt history, versioned skill reference and integrity hash, zero cost cap and actual cost, timeout and retry budget, output manifest, errors and stop reason, the created review task, and an idempotent replay without duplicate execution. HTTP-level replay against the same live server verified conflicting-key 409, cross-business-unit denials for reads/cancel/retry/review tasks, retry-after-success denial, cancel-after-success output preservation, Talent campaign execution with the Talent skill binding, archetype-only output with named-person/rights/brand-safety blocks intact, CSRF and non-loopback-Host rejection, and safe error pages without tracebacks, SQL, or local paths. A real restart against the same state file preserved runs, attempts, outputs, audits, and review tasks while process-local campaign state reset as documented. A 375-pixel horizontal-overflow defect from long hashes was found and fixed (`overflow-wrap:anywhere`), then re-verified overflow-free; keyboard tab focus showed the visible outline; the console had no messages; the network log showed loopback requests only; the server and listener were confirmed stopped with no worker process remaining. Executor-level failure classes (timeout, cancellation mid-run, cost violation, storage/audit faults, interruption recovery) are exercised deterministically in tests with injected clocks, executor fakes, and one-shot fault injection because the real fixture adapter cannot fail on demand in a live session; this is the only live-QA substitution and is reported as such.
- Safety result: the repository-mandated upstream Git fetch was the only external read. No external system was accessed or written, no real, private, or contact data exists, all fixtures remain synthetic and `.example`-scoped, frozen Phase 1 schemas/skills/fixtures, Phase 2 contracts, upstream skill, installers, hooks, and synchronization scripts are unchanged, and all changed paths are inside the Phase 4 allowlist.
- Git result: nothing was staged, committed, pushed, deployed, connected, or sent. Final staging and commit remain Noah's decision. Phase 5 was not started.

### 2026-07-20 — Prospecting Phase 4 correction pass
- Task: Correct the six independently reproduced Phase 4 defects and return the phase for another independent review.
- Phase / status: Phase 4 repository-local Build correction from base commit `8ea7c16`; still pending independent review/finalization. Phase 5 remains unauthorized and was not started.
- Risk / mode: Green / reversible local Build with a temporary loopback application and scratch/temporary SQLite state files used only for verification.
- Defects corrected at root cause: (1) worker attempts previously executed through the Phase 3 process-local repository, so a durable retryable run could not replay after a real restart ("Campaign version not found"); every attempt now validates the run's persisted immutable configuration snapshot (canonical-hash check, frozen Phase 1 revalidation, server-owned business-unit skill binding, recorded skill-integrity manifest) and rehydrates the campaign version through a new synchronized `ensure_campaign_version` boundary, failing closed on corrupted or mismatched durable input, with recovery still manual. (2) Durable outputs and review tasks previously dead-linked into the vanished process-local queue after restart; worker-run and review-task pages now render a read-only, business-unit-scoped durable result projection from the validated snapshot and truthfully label the remaining process-local governed-action limitation. (3) A cross-business-unit idempotency-key collision previously appended an `idempotency_conflict` audit onto the foreign run before authorization; keys remain global by documented contract, but a conflict now authorizes before linking, and an inaccessible collision records only an unlinked, caller-scoped rejection fact with the same generic safe 409, leaving the foreign run and its audit history byte-identical. (4) Concurrent identical submissions could turn the lost find/create race into a bounded 500; run creation is now concurrency-safe at the SQLite boundary (serialized in-transaction existence check plus a typed unique-collision signal) and the loser resolves as an idempotent replay or safe conflict with exactly one logical run and no partial audit; the uniqueness invariant is unchanged. (5) Running cancellation was not reachable through the real single-threaded HTTP surface; the loopback server now handles each request on a bounded, joined thread (no scheduler, poller, watcher, or daemon), the process-local fixture repository is serialized behind a reentrant lock, and a deterministic two-client regression proves an authorized human can observe and cancel a running attempt with cancellation winning exactly once, no partial output or review task, and no worker or listener surviving shutdown. (6) Retry transitions left stale `completed_at` metadata and could record `retry_accepted` without a claimed attempt; the atomic claim now clears terminal-only metadata while preserving first-start history, commits `retry_accepted` inside the same claim transaction, and an audit fault rolls back the acceptance, transition, and attempt insert together.
- Files changed in this pass: `apps/prospecting-mission-control/mission_control/store.py`; `apps/prospecting-mission-control/mission_control/agent.py`; `apps/prospecting-mission-control/mission_control/application.py` (synchronized fixture repository plus the rehydration boundary); `apps/prospecting-mission-control/mission_control/web.py` (bounded threading loopback server, durable result projection, truthful labels); `tests/prospecting/test_phase4_registered_agent.py` (fifteen new regressions in six focused classes); `docs/PHASE_4_REGISTERED_AGENT.md`; `docs/ARCHITECTURE.md`; and this append-only log. No frozen Phase 1, Phase 2 contract, upstream, installer, hook, or synchronization path changed.
- Regression result: new tests cover fresh-process manual retry from persisted validated input, fresh-process recovery retry, corrupted-snapshot and changed-skill fail-closed classes, restart-safe durable output and review-task inspection through real application routes with business-unit isolation, cross-unit key-collision isolation at service and HTTP level with byte-identical foreign audit history, deterministic lost find/create race replay, two-thread identical and conflicting creation invariants with no 500, real two-client loopback HTTP cancellation of a running attempt using deterministic events with no sleeps, stale-completion clearing, lost-claim acceptance-audit absence, and retry-claim audit-fault rollback. Each regression asserts the negation of the independent reproduction transcript for its defect; the pre-correction implementation was uncommitted, so re-execution against it was not possible and is not claimed.
- Verification performed: mandatory upstream check and validated dirty no-op synchronization before editing; 49 targeted Phase 4 tests (34 original plus 15 new); 35 Phase 3 tests; the complete 126-test repository suite; both official skill validators (`Skill is valid!`); Python compilation; all fixture JSON parsing; shell syntax including `run_all.sh`; `git diff --check`; SQLite `integrity_check`/`foreign_key_check` on a temporary corrected state file (ok, none); protected-path equality against `8ea7c16`; staged/conflict/runtime-artifact checks; scoped credential/personal-data/network/shell/scheduler/destructive-SQL/sleep/path searches over the changed surface (only test negative-assertion strings matched); and complete final-diff review including untracked files.
- Browser/HTTP QA result: a fresh server process was started over the prior durable QA state file; the review-task and worker-run pages rendered the complete durable validated result projection with the truthful process-local limitation label, the process-local queue was empty as documented, cross-business-unit access to the durable pages failed closed, the 375-pixel layout had no horizontal overflow, the console had no messages, and the listener and process were confirmed gone after shutdown. Real in-flight HTTP cancellation is exercised by the deterministic two-client regression with a gated executor; the live fixture adapter completes too quickly to cancel interactively, and that substitution is reported honestly.
- Safety result: the repository-mandated upstream Git fetch was the only external read; no external system was accessed or written; no real, private, or contact data exists; all fixtures remain synthetic and `.example`-scoped; no scheduler, recurring loop, watcher, daemon, creative, likeness, advertisement, outreach, deployment, or Phase 5 behavior was added.
- Git result: nothing was staged, committed, pushed, deployed, connected, or sent. Final staging and commit remain Noah's decision after independent review.

- Continuation verification: a focused same-process reproduction found that the reviewed skill-integrity manifest was cached after run creation, so a retry could miss a subsequent local skill-content change even though fresh-process mismatch handling already failed closed. The cache was removed, every governed boundary now recomputes the manifest, and a new regression covers same-process retry mismatch. Actual continuation results were 50 Phase 4 tests, 35 Phase 3 tests, and 127 total tests; both official skill validators passed with the existing local PyYAML path. No other correction behavior or historical entry was changed.
- Continuation correction: fixture execution now stages a non-visible Phase 3 result and projects it exactly once only after durable success, so cancellation, cost/validation rejection, and output/review-task rollback leave no Phase 3 run, prospects, idempotency entry, or manual-run audit; `cost_limit_exceeded` now atomically records the executor-reported cost on both run and attempt without fabricating absent cost. Actual results: 57 Phase 4 tests, 35 Phase 3 tests, and 134 total tests passed; both official skill validators passed. Phase 4 remains pending independent review/finalization, and Phase 5 remains unauthorized.
- Continuation correction: the post-executor boundary now uses the repository's concrete non-mutating prepared-result validator before durable success and again before process-local commit, so nested corruption and fabricated `existing_run` claims become completed safe failures while a repository-backed run with matching idempotency provenance settles without duplication; durable snapshot reads verify stored length/SHA-256, later snapshots preserve governed review events, and durable dispositions prioritize suppression then governed decisions. Actual results: 63 Phase 4 tests, 35 Phase 3 tests, and 140 total tests passed; both official skill validators passed. Phase 4 remains pending independent review/finalization, and Phase 5 remains unauthorized.

### 2026-07-20 — Prospecting Phase 5 weekly shadow-loop implementation
- Task: Implement the authorized program-roadmap Phase 5 Scheduled Loop in Shadow Mode on the independently finalized Phase 4 control plane at commit `b340b2c013b2fd026cd0be3aff24429196bd9be8`.
- Phase / status: Phase 5 repository-local Build; explicitly authorized and implemented locally, pending independent review/finalization. Phase 6 remains unauthorized.
- Risk / mode: Green / reversible local Build using only synthetic fixtures, temporary SQLite files, and a temporary loopback server for verification.
- Implementation files: `apps/prospecting-mission-control/mission_control/shadow.py`; `apps/prospecting-mission-control/mission_control/store.py`; `apps/prospecting-mission-control/mission_control/agent.py`; `apps/prospecting-mission-control/mission_control/application.py`; `apps/prospecting-mission-control/mission_control/web.py`; `fixtures/prospecting/phase5/history-seeds.json`; and `tests/prospecting/test_phase5_shadow_loop.py`.
- Documentation and policy files: `docs/PHASE_5_SHADOW_LOOP.md`; `docs/ARCHITECTURE.md`; `docs/PHASE_4_REGISTERED_AGENT.md`; `docs/specs/UNREAL_PROSPECTING_SKILLS_PROGRAM_ROADMAP.md`; `AGENTS.md`; `UNREAL_README.md`; and this append-only log.
- Architecture result: one disabled-by-default, human-configured weekly UTC shadow loop references approved immutable campaign versions, rotates deterministically across filtered versions and configured open-discovery weeks, claims at most one due occurrence per evaluation, advances directly to a future due time, and derives stable schedule-occurrence idempotency. It reuses `RegisteredAgentService` for exact-zero-cost fixture execution, bounded attempts, cancellation, durable output, audit, and pending human-review task creation. Frozen history validation and durable same-business-unit result facts run before execution; missing, corrupt, ambiguous, or inconsistent history fails closed. Atomic versioned enable/pause/resume/disable transitions, occurrence claims, local alerts, health, duplicate rate, and restart recovery are persisted additively. The bounded scheduler and request threads are explicitly stopped and joined; no generic cron, catch-up loop, worker replacement, or Phase 6 capability was added.
- SQLite result: repository-local schema version 2 adds only Phase 5 schedule, rotation, occurrence, alert, and audit tables/indexes over the finalized Phase 4 schema. Fresh initialization and a populated actual Phase 4 schema/state upgrade were tested; all Phase 4 rows, IDs, attempts, outputs, review tasks, audits, and manifests remained unchanged. No `DROP`, `TRUNCATE`, destructive reconciliation, external migration, database connection, or migration application occurred. A fresh v2 state returned `integrity_check=ok` and no `foreign_key_check` rows.
- Verification performed: mandatory clean preflight and validated upstream synchronization no-op; 17 focused Phase 5 tests, 63 Phase 4 tests, 35 Phase 3 tests, and the complete 157-test repository suite; both official skill validators (`Skill is valid!`); Python compilation with external pycache; 22 fixture/schema JSON parses; shell syntax; fresh/upgrade SQLite checks; concurrency, idempotency, pause/disable race, restart, history, business-unit, CSRF, alert, and shutdown regressions; `git diff --check`; protected-path equality against the Phase 4 base; staged/conflict/runtime-artifact checks; scoped privacy, credential, external-client, dynamic/shell-execution, destructive-SQL, scheduler-lifecycle, creative, likeness, advertisement, and outreach searches; and complete tracked-plus-untracked diff review. Review corrections fixed durable occurrence rendering, duplicate-rate summary projection, generic cross-business-unit schedule non-disclosure, and safe local-alert rendering.
- Browser/HTTP QA result: a real server on `127.0.0.1` exercised synthetic UMG campaign and disabled schedule creation, explicit enable/pause/resume/disable, a genuinely due scheduler occurrence, durable restart inspection, zero cost, partial-source stop reason, duplicate rate, one pending review task, the empty local-alert state, and generic Talent-to-UMG denial. Mobile, tablet, and desktop layouts remained usable; keyboard focus had a visible three-pixel outline; labels and live status/error regions were present; the final console was clean; the final network trace contained only the loopback request. The schedule was disabled, the server stopped, and process inspection found no Mission Control, scheduler, or QA browser process remaining.
- Safety result: all records and histories are synthetic and local. The repository-mandated upstream Git validation was the only external access. No external system or database was accessed or written; no real record, private contact value, credential, live research, scraping, creative, likeness, advertisement, outreach, deployment, or Phase 6 behavior was added or executed. Frozen Phase 1 assets, finalized Phase 2 contracts, upstream skill, installers, hooks, and synchronization scripts remained unchanged.
- Git result: nothing was staged, committed, pushed, deployed, connected, or sent. Final staging and commit remain Noah's decision after independent review.

### 2026-07-20 — Prospecting Phase 5 correction pass
- Task / status: Correct the six independently reproduced Phase 5 defects on base `b340b2c013b2fd026cd0be3aff24429196bd9be8`. Phase 5 remains pending independent review/finalization; Phase 6 remains unauthorized.
- Corrections: preserved the finalized Phase 4 manual request fingerprint while namespacing scheduled requests; made scheduled acceptance/rejection audits truthful; settled unreadable history and transient post-claim store/output failures without killing the scheduler or stranding a claim; defined duplicate count as governed identity/history classifications over all result rows; reused one bounded output-integrity decoder and one metric helper for normal completion and recovery; made scheduled run creation/link/audit atomic, Noah-only, and exact-scope matched; prohibited recovered scheduled retries after terminal occurrence settlement with an atomic claim guard; invalidated a succeeded run and pending review task when its recovered output fails integrity; and rendered Rob/Dan schedule views and terminal retry/metric states truthfully without mutation controls. The finalized Phase 4 manual retry and positional web-constructor contracts remain intact.
- Regression evidence: before correction, focused negative tests reproduced the legacy manual replay 409, manual-labeled scheduled rejection, escaping `IsADirectoryError` with a claimed occurrence/no alert, duplicate count 10 instead of 2, tampered output completing with count 0, run/occurrence retry split truth, and Rob mutation controls. The corrected suite adds direct coverage for those transcripts plus exact mixed duplicate examples, create/link rollback, forged actor/business-unit/campaign links, retry/recovery interleaving, transient settlement and output-read reconciliation, post-output audit rollback, cross-service durable claiming, legacy positional construction, warning-free connection closure, and invalid output run/task settlement.
- Verification: Phase 5 `31/31`, Phase 4 `63/63`, Phase 3 `35/35`, and `PYTHONPATH=/tmp/unreal-prospecting-pyyaml bash tests/prospecting/run_all.sh` `171/171` passed; the focused Phase 5 run also passed with `ResourceWarning` promoted to error. Both official skill validators reported `Skill is valid!`; external-pycache compilation, 24 JSON parses, shell/Node syntax, fresh v2 and populated v1-to-v2 `integrity_check=ok` with zero foreign-key rows, real loopback CSRF/BU/read-only/control/shutdown checks, tracked-plus-untracked `git diff --check`, protected-path equality, runtime/conflict/index/privacy/secret/contact/outbound/dynamic-execution/destructive-SQL/Phase-6 scans, and the gstack pre-landing specialist review passed.
- Safety / Git: only the authorized Phase 5 correction surface changed; existing Phase 5 fixture/prose changes were preserved. Generated Python caches were removed. No external system, real record, credential, live research, scraping, creative, likeness, advertising, outreach, deployment, or Phase 6 capability was accessed or added. Nothing was staged, committed, pushed, deployed, connected, or sent.

### 2026-07-20 — Prospecting Phase 5 scheduled-retry race correction
- Task / status: Close the independently reproduced claim-before-settlement race on base `b340b2c013b2fd026cd0be3aff24429196bd9be8`. Phase 5 remains pending independent review/finalization; Phase 6 remains unauthorized.
- Correction: every worker run immutably linked to a shadow occurrence now rejects manual retry with a bounded `409` before another attempt can be claimed, including while the occurrence remains claimed. The occurrence exclusively owns final settlement; worker-run detail projects that effective no-retry policy without changing stored or behavioral Phase 4 manual-run retry facts. A deterministic gated two-thread regression reproduced the old second-claim path before correction and now proves denial, exactly one attempt and occurrence settlement audit/alert, no output, and no review task; the ordinary Phase 4 manual retry regression remains green.
- Verification: Phase 5 passed `32/32` with `ResourceWarning` promoted to error, Phase 4 passed `63/63`, Phase 3 passed `35/35`, and `PYTHONPATH=/tmp/unreal-prospecting-pyyaml bash tests/prospecting/run_all.sh` passed `172/172`; both official skill validators reported `Skill is valid!`. External-pycache compilation, 24 JSON parses, shell/Node syntax, fresh v2 and populated v1-to-v2 SQLite integrity/foreign-key checks, loopback HTTP/security/shutdown coverage, protected-path equality, `git diff --check`, runtime/index/conflict/privacy/outbound/destructive-SQL/Phase-6 scans, and the gstack pre-landing review passed.
- Safety / Git: the exact authorized Phase 5 dirty path set was preserved. No external system, real data, credential, live research, scraping, creative, likeness, advertising, outreach, deployment, or Phase 6 capability was accessed or added. Nothing was staged, committed, pushed, deployed, connected, or sent.

### 2026-07-21 — Prospecting Phase 5 metadata finalization correction
- Task / status: Reconcile current governing metadata after independent review. Phase 4's current-lineage finalized commit is `b7892aed5ad9ab0dcb9d900d95d6971332582ef8`; historical commit `b340b2c013b2fd026cd0be3aff24429196bd9be8` has the same parent and tree but is not an ancestor of current HEAD. Phase 5 is independently reviewed and finalized at `1ddba9037f1db7e1b58098b83f7bf4caeafd7e8d`. Phase 6 remains unauthorized.
- Independent-review evidence supplied by the root reviewer: the committed Phase 5 tree had the exact intended 14-path change set, the complete repository suite passed `172/172`, and both official skill validators passed. This metadata pass does not represent those root checks as locally rerun.
- Verification performed locally: mandatory `check-upstream.sh` and safe no-op `sync-upstream.sh` confirmed clean `unreal` at `1ddba9037f1db7e1b58098b83f7bf4caeafd7e8d` with synchronized `main`; `PATH=/usr/local/bin:/opt/homebrew/bin:$PATH PYTHONPATH=/tmp/unreal-prospecting-pyyaml bash tests/prospecting/run_all.sh` passed `172/172`; and both official validators passed with PyYAML installed only under `/tmp/unreal-prospecting-pyyaml-full`. Documentation/status reconciliation remains limited to the authorized metadata surface.
- Safety / Git: no application behavior, test, fixture, schema, skill, sync-script, Phase 2 contract, or Phase 6 work changed. No external system, real record, credential, live research, scraping, creative, likeness, advertising, outreach, deployment, or autonomous work was accessed or begun. Nothing was staged, committed, pushed, deployed, connected, or sent.

### 2026-07-21 — Prospecting Phase 6A synthetic enrichment and campaign brief implementation
- Task / status: Implement the explicitly authorized Phase 6A repository-local synthetic enrichment and campaign-brief boundary from clean finalized Phase 5 base `29edc735ae442154393f2266a3c7b6c04b537e8e`. The implementation is complete locally and pending independent review/finalization. Phase 6B, official-site/live research, and every later phase remain unauthorized.
- Risk / mode: Green / reversible local Build using only repository-owned synthetic fixtures, `.example` metadata, temporary SQLite files, and a temporary loopback server. Fixture actor selection remains policy simulation, not authentication or production authorization.
- Implementation files: `apps/prospecting-mission-control/mission_control/enrichment.py`; additive changes to `apps/prospecting-mission-control/mission_control/store.py`; loopback routes and readable UI changes in `apps/prospecting-mission-control/mission_control/web.py`; `fixtures/prospecting/phase6/brief-fixtures.json`; and `tests/prospecting/test_phase6_enrichment_brief.py`.
- Documentation and policy files: `docs/PHASE_6_ENRICHMENT_BRIEF.md`; `docs/ARCHITECTURE.md`; `docs/PHASE_5_SHADOW_LOOP.md`; `docs/specs/UNREAL_PROSPECTING_SKILLS_PROGRAM_ROADMAP.md`; `AGENTS.md`; `UNREAL_README.md`; and this append-only log.
- Approval and atomicity result: an append-only event is exactly bound to one pending review task, successful worker run, immutable output hash/length, canonical result hash/length, global identity, business unit, protection hash, scope `phase6a_fixture_enrichment`, proposer, separate allowed reviewer, bounded reason, effective/recorded timestamps, exact seven-day expiry, and optional superseded leaf. Process-local `approve_deeper_research` does not authorize Phase 6A. Current leaf, validity, task/output/result integrity, identity, duplicate state, suppression, rejection, relationship, rights, brand safety, and named-person protection are rechecked under `BEGIN IMMEDIATE`. Concurrent supersessions have one winner and a safe `409` loser. Concurrent identical starts create one logical single-use run; an identical replay is read-only. Revoke-first blocks a claim, while claim-first permits only the already-claimed attempt to finish and makes later revocation non-retroactive. Audit failure rolls back the associated transition. Interrupted claims become visible terminal failures on restart and cannot retry.
- Brief result: one bounded validator reads no network and accepts only repository-owned synthetic profiles with non-official `.example` URLs, no URL credentials/query/fragment, bounded summaries, quotes of at most 25 words, observation/source dates, and recursively checked fixture values. UMG and archetype-only Talent briefs contain every roadmap research field plus unit-specific requirements. Every material value carries basis, evidence, confidence reason, uncertainty, freshness/currentness, and source lineage; current-campaign dates are checked; conflicts and unknowns remain explicit; no page body is stored. Talent output cannot name a person or claim availability, endorsement, clearance, or likeness permission. Append-only brief review grants only `research_quality_only`, never generation or execution authority.
- SQLite result: Phase 6A activation deterministically upgrades the finalized Phase 5 v2 store to additive v3 before any Phase 6A read/write. Normal `WebApplication` construction always activates it, and v3 reopen is idempotent. Six minimal tables persist approval events, single-use runs, bounded sources, immutable brief versions, evidence links, and research-quality review events while reusing the existing audit table. Fresh activation and populated actual v2 upgrade preserve every earlier row and byte; no destructive SQL, external migration, or database connection exists.
- Verification: the 17 focused Phase 6A tests cover durable authority separation, exact expiry before/at/after the boundary, business-unit/result/identity/protection/integrity failures, self-approval/review, supersession/fork/concurrent-decision behavior, both revocation/claim orders, concurrent starts, idempotent replay, single use, audit rollback, restart recovery, UMG/Talent completeness, source restrictions, tamper detection, append-only research review, fresh/reopen/populated upgrade, real loopback HTTP security/shutdown, and prohibited capability scans. Final repository verification passed 189/189 tests (172 finalized regressions plus 17 new tests), with both official skill validators reporting `Skill is valid!`; Python external-pycache compilation, all JSON parsing, shell syntax, SQLite integrity/foreign-key checks, `git diff --check`, allowed/protected-path checks, staged/conflict/runtime-artifact checks, and scoped privacy/credential/contact/outbound/dynamic-execution/destructive-SQL/later-phase scans passed.
- Browser QA: a real server bound to `127.0.0.1` loaded the approval, enrichment, and complete brief views; a real research-quality form submission persisted and rendered its `research_quality_only` event and no stronger authority. Desktop and 375-pixel layouts were usable with no horizontal overflow, fields and controls were labeled, status semantics were present, the console was clean, and no external link existed. The server, scheduler/request threads, listener, browser state, temporary database, and generated cache artifacts were stopped or removed.
- Workflow / minimalism: the gstack engineering-planning, security, pre-landing review, and constrained QA workflows were applied. Ponytail review kept the implementation to one direct standard-library service, one additive schema set, one fixture, one test module, existing audit/HTTP/security patterns, and no dependency, parallel subsystem, retry framework, scheduler, network client, or speculative configuration.
- Safety / Git: mandatory upstream check and safe synchronization no-op passed before editing; the upstream Git preflight was the only external read. No external system or database, real record, private/contact data, credential, official site, live research, scraping, generation system, creative, likeness, advertisement, outreach, deployment, schedule, daemon, or later phase was accessed or added. Frozen Phase 1 assets, finalized Phase 2 contracts, upstream skill, installers, hooks, remotes, and unrelated paths remain unchanged. Nothing was staged, committed, pushed, deployed, connected, or sent.

### 2026-07-21 — Prospecting Phase 6A independent-review correction pass
- Task / status: Correct the independently reproduced Phase 6A authority, integrity, concurrency, restart, business-unit, and review-provenance defects on finalized Phase 5 base `29edc735ae442154393f2266a3c7b6c04b537e8e`. Phase 6A remains a local pending-review implementation; Phase 6B and all later phases remain unauthorized.
- Authority and concurrency corrections: approval creation and claim now bind and recheck the canonical worker configuration, exact task/run/output/result graph, current governed protection projection, immutable proposer and separate reviewer, and the exact seven-day effective-time window. A stale or unavailable process-local projection is visible only as non-actionable history. The bound run proposer alone may consume an approved leaf. The projection lock is held through approval/claim commit, current-leaf listing revalidates eligibility, and a process-local active-owner registry prevents a second service instance from falsely recovering a live synchronous claim while preserving genuine restart recovery and claim-first/revocation-first ordering.
- Integrity and isolation corrections: recursive fixture validation covers mapping keys, ignored fields, bounded nesting, contact/credential material, malformed evidence identifiers, and malformed source URLs before persistence. Approval/review events and source, evidence, and brief artifacts have database immutability triggers. Sources and evidence links store and recheck canonical snapshots, hashes, lengths, denormalized fields, and cross-record lineage before listing, rendering, or review. Brief top-level bindings and immutable review provenance are rechecked; business-unit-qualified brief families prevent shared-global-identity version collisions.
- Verification: Phase 6A passed `29/29`; the final complete repository suite passed `201/201`; both official skill validators reported `Skill is valid!`. External-pycache compilation, 18 fixture/schema JSON parses, shell syntax, fresh v3 and populated v2-to-v3 SQLite integrity/foreign-key coverage, tracked and untracked whitespace checks, protected-path/index/conflict/runtime-artifact checks, and scoped privacy, credential, contact, outbound-client, dynamic-execution, destructive-SQL, scheduling, generation, outreach, and later-phase scans passed. Real loopback browser QA covered desktop/mobile layout, keyboard focus, console, loopback-only requests, durable rendering, integrity-failure projections, and joined shutdown before the final authority-display refinement; the final proposer-only display and action boundary is covered by the green HTTP regression.
- Safety / Git: only the authorized 12-path Phase 6A surface is dirty. No external system, real data, credential, official-site/live research, scraping, creative, likeness, advertising, outreach, deployment, recurring worker, or later-phase capability was accessed or added. No runtime artifact or process remains. Nothing is staged, committed, pushed, deployed, connected, or sent; final independent review and shipping remain with the root reviewer.

### 2026-07-21 — Lead-intelligence phase-map correction
- Task / scope: Reframe this repository as the upstream finder and comprehensive customer-research node in a larger governed agent graph. Public business people and contact paths are one dossier category, not the whole product. The authorized change is documentation and ownership mapping only; the existing Phase 6A application, test, and fixture implementation remains unchanged and pending independent review.
- Mapping result: Phase 6 now ends with a comprehensive, source-backed customer dossier and reviewed, versioned, machine-readable graph handoff. The mapped dossier covers identity and relationships, commercial context, products and services, audiences and markets, brand and campaign evidence, public asset references, activity and change signals, opportunities and fit, competitors, public business people/roles/contact paths, governance and history, and evidence coverage with explicit gap states. Phase 6A remains the narrow synthetic brief foundation; Phase 6B remains unauthorized and must begin contract-first before any approved live public read.
- Downstream boundary: preview/image/video generation, talent or campaign execution, recipient verification and outreach, CRM or external persistence, graph orchestration, and outcome learning are no longer repository phases. They are separately owned graph consumers that may use a human-released intelligence package but receive no authority from it. This repository does not implement or modify Unreal OS and does not invoke those consumers.
- Verification: the focused Phase 6A suite passed `34/34`; the complete repository suite passed `206/206`; both official skill validators reported `Skill is valid!`; `git diff --check`, changed-document link existence, phase-heading review, and obsolete Phase 7–9/current-scope terminology searches passed. The first complete-suite wrapper attempt reached `206/206` tests but used an incomplete temporary YAML import for its validator step; the full wrapper was rerun with the complete temporary PyYAML path and exited successfully with both validators green.
- Safety / Git: the mapping preserves intentionally public business-source, evidence-lineage, privacy, rights, business-unit, duplicate/history, and human-review requirements. It prohibits private or sensitive data, guessed contacts, paid enrichment, authenticated scraping, asset copying/generation, outreach, external writes, and unsupported inference. The mandatory upstream check and synchronization passed as a dirty no-op before editing. No application code, fixture, test, finalized Phase 2 contract, external system, real record, network research source, or downstream agent was changed or accessed. Nothing was staged, committed, pushed, deployed, connected, or sent.

### 2026-07-21 — Prospecting Phase 6A final correction continuation
- Task / status: Correct the seven independently reproduced Phase 6A defects remaining on base `29edc735ae442154393f2266a3c7b6c04b537e8e`. Phase 6A remains unstaged and pending another independent review; Phase 6B and every later phase remain unauthorized.
- Corrections: governed rejection and unknown decision states now block durable approval and claim until a valid append-only process-local supersession; research-quality acceptance requires the latest business-unit-qualified brief version, ready state, and no unresolved conflict; an immutable exact ordered-set manifest independently detects missing, extra, deleted, reordered, rebound, or altered source/evidence rows; UMG and Talent fixtures use exact typed schemas with normalized credential/contact-key checks, no percent encoding, and no non-finite JSON constants; completion storage or audit rollback records bounded truthful failure metadata without partial source/evidence/brief rows; the proposer receives a read-only separation-of-duty brief view while an allowed different reviewer retains the labeled form; and documentation now limits the active-claim registry to the supported single-process threaded server.
- Files changed by this continuation: `apps/prospecting-mission-control/mission_control/enrichment.py`; `apps/prospecting-mission-control/mission_control/store.py`; `apps/prospecting-mission-control/mission_control/web.py`; `tests/prospecting/test_phase6_enrichment_brief.py`; `docs/PHASE_6_ENRICHMENT_BRIEF.md`; `docs/ARCHITECTURE.md`; and this append-only log. The other existing Phase 6A dirty paths were preserved.
- Verification: the test-first run initially reproduced the defects with 15 failures and 2 errors. After correction, Phase 6A passed `34/34`, Phase 5 `32/32`, Phase 4 `63/63`, and Phase 3 `35/35`. `PYTHONPATH=/tmp/unreal-phase6a-validator bash tests/prospecting/run_all.sh` passed `206/206`, and both official skill validators reported `Skill is valid!`; the temporary validator path reused already-present CPython 3.14 PyYAML bytecode and installed no dependency. External-pycache compilation, all 24 repository JSON parses, shell syntax, fresh v3, populated v2-to-v3 byte preservation, v3 reopen, `PRAGMA integrity_check=ok`, empty `foreign_key_check`, privacy/credential/contact/outbound/dynamic-execution/destructive-SQL/scheduling/generation/outreach scans, protected-path review, and full tracked-plus-untracked review passed.
- Browser QA: a real `127.0.0.1` server exercised campaign and worker creation, separated durable approval, one zero-cost fixture enrichment, proposer read-only state, reviewer acceptance with `research_quality_only`, desktop and 375-pixel layouts, keyboard-visible focus, a deliberate temporary evidence-link deletion producing a safe `409`, and cross-business-unit disclosure denial producing a generic `403`. Expected 409/403 requests were the only console errors; a clean-page replay had no console errors, and every captured request used `127.0.0.1`. Chromium, the server, scheduler/request threads, and the listener were stopped. Browser-tool `.gstack` metadata was removed and `.gitignore` was restored to its preflight bytes.
- Safety / Git: mandatory upstream validation and synchronization completed as the documented dirty no-op. That Git validation was the only external read. No live site, external application or database, real record, private contact value, credential, Phase 6B research, generation, likeness, advertisement, outreach, deployment, recurring worker, or later-phase capability was accessed or added. No external write occurred. Nothing was staged, committed, pushed, deployed, connected, or sent; final independent review and shipping remain the user's decision.

### 2026-07-21 — Prospecting Phase 6A exact-target and event-integrity correction
- Task / status: Correct four independently reproduced Phase 6A integrity gaps on finalized Phase 5 base `29edc735ae442154393f2266a3c7b6c04b537e8e`. Phase 6A remains unstaged and pending independent review; Phase 6B and all later phases remain unauthorized.
- Corrections: every synthetic fixture profile now carries an exact business-unit/result/global-identity/account/domain target that must match the durable approved result before any enrichment artifact is persisted; every research field and evidence-reference list now has an exact bounded type and canonical identifier contract, with `explicit_unknown` reserved for null values; canonical snapshots, hashes, and lengths independently authenticate approval and brief-review events before authority, listing, rendering, claiming, or further review; and schema-v3 layout validation fails closed for local pre-correction v3 files that cannot satisfy those new event-integrity columns, with an explicit archive-and-recreate policy instead of silently trusting legacy state.
- Regression evidence: the initial four focused tests reproduced all defects with 13 assertion failures. The corrected four-test run passed, the complete Phase 6A module passed `40/40`, Phase 5 passed `32/32`, Phase 4 passed `63/63`, Phase 3 passed `35/35`, and the complete repository suite passed `212/212`. Both official skill validators reported `Skill is valid!`; external-pycache compilation, strict parsing of 25 JSON files, syntax validation of three shell files, fresh v3, populated actual v2-to-v3 upgrade, v3 reopen, pre-correction-v3 fail-closed behavior, `PRAGMA integrity_check=ok`, and empty `foreign_key_check` passed.
- Browser and security evidence: a real loopback server exercised exact UMG and Talent targets, zero-cost enrichment, proposer read-only and reviewer-only research-quality acceptance, safe target-mismatch failure with no partial artifacts, forged approval/review event rejection, desktop and 375-pixel layouts, keyboard-visible focus, form labels, live error semantics, a clean console replay, and loopback-only network requests. The browser, server, request threads, and listener were stopped. Complete code/security review found no executable external client, credential/contact data, shell or dynamic execution, destructive SQL, recurring scheduler, generation, likeness, advertisement, outreach, Phase 6B behavior, or repository runtime artifact.
- Files changed by this correction: `apps/prospecting-mission-control/mission_control/enrichment.py`; `apps/prospecting-mission-control/mission_control/store.py`; `fixtures/prospecting/phase6/brief-fixtures.json`; `tests/prospecting/test_phase6_enrichment_brief.py`; `docs/PHASE_6_ENRICHMENT_BRIEF.md`; `docs/ARCHITECTURE.md`; and this append-only log. The four lead-intelligence mapping files and every other pre-existing dirty path were preserved without continuation edits.
- Safety / Git: mandatory upstream validation and synchronization passed as the documented dirty no-op; that repository Git validation was the only external read. No external system or database, live site, real record, private contact value, credential, Phase 6B research, generation, likeness, advertisement, outreach, deployment, recurring worker, or later-phase capability was accessed or added. No external write occurred. Nothing was staged, committed, pushed, deployed, connected, or sent; independent review and final shipping remain the user's decision.

### 2026-07-21 — Prospecting Phase 6A trusted-local-state integrity-scope correction
- Task / status: Narrow the Phase 6A local SQLite integrity contract to what the lead-intelligence product actually requires, on finalized Phase 5 base `29edc735ae442154393f2266a3c7b6c04b537e8e`. Remove the inaccurate claim that adjacent snapshots/hashes independently authenticate authority after a trusted operator directly rewrites a database event and its colocated digest. Phase 6A remains unstaged and pending independent review; Phase 6B and all external research remain unauthorized.
- Locked trust decision: the local SQLite file and the operating-system account that owns it are trusted. All normal, enforced protections are preserved — endpoint authorization and business-unit isolation; exact result-bound, seven-day human approval with proposer/reviewer separation; serialized current-leaf transitions and single-use claims; append-only and immutability triggers; transactions, foreign keys, constraints, idempotency, and rollback; and canonical snapshots, hashes, and lengths for accidental-corruption and partial-mutation detection. No HMAC, signature service, secret, key management, second ledger, or external trust anchor was added, and no ordinary application path depends on database-owner tamper resistance.
- Independent-review evidence corrected: the prior wording claimed forged or self-consistent direct row changes fail closed. Independent review demonstrated that a self-consistent authority-event edit plus a recomputed colocated digest is accepted; the colocated digest detects partial or inconsistent corruption but is not independent authentication of authority. This entry does not amend the earlier "independently authenticate" wording in prior log entries, which remain append-only; the corrected contract is documented in the design docs below.
- Documentation corrections: `docs/PHASE_6_ENRICHMENT_BRIEF.md` corrects the two "forged or self-consistent direct row changes fail closed" / "self-consistent row alteration ... fails closed" claims to accurate accidental-corruption and partial-mutation detection, adds an explicit "Trusted local-state boundary" section, rewords "tampered bytes" to "bytes inconsistent with their colocated snapshot", and reframes the independent-review focus bullets. `docs/ARCHITECTURE.md` adds a trusted-local-state paragraph making the same boundary explicit. The useful claim that hashes detect accidental corruption and inconsistent or partial row changes is preserved.
- Test corrections: replaced the tests that deliberately disabled database safeguards (`DROP TRIGGER` / `ALTER TABLE DROP COLUMN` followed by a row rewrite) with defensive tests that (a) verify the immutability triggers reject ordinary updates and deletes across all six immutable Phase 6A tables, (b) exercise the pure integrity validators (`_validate_event_integrity`, `EnrichmentService._validated_source`, `EnrichmentService._validated_link`) with inconsistent copied records, and (c) construct the legacy pre-correction schema-v3 layout directly in a temporary database instead of dismantling a valid store. The exact-set manifest completeness check is now demonstrated with an ordinary appended row rather than a trigger-disabled deletion, and rejection-grants-no-authority is proved on the real application path. Equivalent normal-path coverage is retained; endpoint authorization, separation of duty, atomic claims, business-unit isolation, exact fixture targeting, type validation, and artifact completeness checks were not weakened.
- Files changed by this correction: `tests/prospecting/test_phase6_enrichment_brief.py`; `docs/PHASE_6_ENRICHMENT_BRIEF.md`; `docs/ARCHITECTURE.md`; and this append-only log. Application and schema files (`enrichment.py`, `store.py`, `web.py`) and the fixture were unchanged; no ordinary application-path regression required a code correction. Every other pre-existing Phase 6A dirty path was preserved without continuation edits.
- Verification: the focused Phase 6A suite passed `41/41` (was `40/40`; one integrity test was split into a trigger-enforcement test and a pure-validator test, net `+1`); Phase 5 `32/32`, Phase 4 `63/63`, Phase 3 `35/35`; the complete repository suite passed `213/213` via `PYTHONPATH=/tmp/unreal-prospecting-pyyaml-full bash tests/prospecting/run_all.sh` and both official skill validators reported `Skill is valid!`. External-pycache `compileall`, strict parsing of 24 tracked-plus-untracked JSON files, shell `bash -n` syntax checks, fresh-v3 and populated actual v2-to-v3 upgrade with `PRAGMA integrity_check=ok` and empty `foreign_key_check`, v3 reopen, direct legacy pre-correction-v3 fail-closed behavior, `git diff --check`, empty index, no merge conflicts, and no repository runtime artifact (no stray `.sqlite3`/`.pyc`/`__pycache__`) all passed. The temporary PyYAML path reused an already-present install and added no repository dependency.
- Safety / Git: mandatory `check-upstream.sh` and `sync-upstream.sh` completed as the documented dirty no-op before editing; that Git preflight was the only external read. Synchronization was a safe dirty no-op, and no unrelated user changes were reset, cleaned, stashed, discarded, overwritten, staged, or committed. No external system or database, live site, real record, private contact value, credential, Phase 6B or live research, generation, likeness, advertisement, outreach, deployment, recurring worker, or later-phase capability was accessed or added. Nothing was staged, committed, pushed, deployed, connected, or sent; independent review and final shipping remain the user's decision.
